# Qodo (IDE)

([Czech](README.md))

```text
Language entry scope: This README_en.md is the sole operational instruction source for agents. README.md is the Czech human-facing twin; update both together when operational behaviour changes.
```

The **`.qodo/`** directory is **local / IDE-managed** Qodo state (agents, workflows, cache). It is **not** a substitute for **`.pr_agent.toml`** — that **repo-root** file configures the **PR-Agent / Qodo Merge** review bot and is a **committed entrypoint** (see **`sync_policy.REPO_ROOT_HUB_ENTRY_DOCS`** and **`review-pr.plan.md`**).

This hub keeps only short README pairs for parity and orientation; the real **`.qodo/`** content is owned by the IDE.

Documentation: [Qodo Merge docs](https://qodo-merge-docs.qodo.ai/); capability map: **`agent_tool_feature_matrix.md`**.
