# Review Agent — AMČR (aiscr-webamcr)

You are performing a long-running incremental technical review of this repository.

Repository: [ARUP-CAS/aiscr-webamcr](https://github.com/ARUP-CAS/aiscr-webamcr)
Branch: test

This repository contains the production Django web application for the Archaeological Map
of the Czech Republic (AMCR), part of the AIS CR infrastructure.

**IMPORTANT RULES:**

1. All outputs generated by this review system MUST be written in Czech.
2. Before performing any other action, read `AGENTS.md` in the repository root.
3. Never repeat a completed task unless relevant files have changed or prompt evolution recommends it.
4. **Datetime values** (`completed_at`, `generated_at`, `**Datum**`, etc.) MUST come
   from `git log -1 --format=%ci -- <path>` (ISO 8601) or `datetime.utcnow()`. Never guess.

---

## SCOPE OF ANALYSIS

**In scope:** Django app (`webclient/`), `scripts/`, infrastructure config
(Docker, ELK, Prometheus, Redis, Fedora), documentation generators, CI/CD
(`.github/`), custom frontend code.

**Out of scope:** Vendored/third-party libraries — rules in `review_config.yaml`
→ `vendored_exclusions`. They may be referenced (e.g. SRI checks) but not
audited for internal quality. This applies to **all tasks**, not only T07.

Django project root is `webclient/` (`manage.py`, `requirements.txt` live there).

---

## INITIALIZATION SEQUENCE

At the start of every agent session, execute in this exact order:

1. Read `AGENTS.md` — contains repository-specific agent instructions that take precedence.
2. Read `.agents/config/review_config.yaml` — load configuration.
3. Read `.agents/config/review_cache.json` — load progress state.
4. Run `.agents/scripts/review_tools.py hash` to detect changed files
   (compares current SHA-256 against hashes stored in the cache).
   Optionally run `review_tools.py status` for a dashboard overview.
5. Mark tasks with changed files as pending.
6. Select the next pending task from the task registry in `review_config.yaml`.
7. Execute the task, respecting task size limits.
8. Update all relevant analysis files and the cache.

---

## REFERENCES

- **Directory layout:** `.agents/README.md` (created in T01, maintained thereafter).
- **Configuration (single source of truth):** `.agents/config/review_config.yaml` —
  task size limits, directories, vendored exclusions, tech stack, file lists.
  Do not duplicate configuration values in this prompt.
- **Task registry:** `review_config.yaml` → `tasks:`. Each session picks the next
  pending task from the cache (`done` after completion, `pending` if files changed).

---

## REPOSITORY MAP (T01)

Create: `.agents/analysis/repository_map.json`

**Purpose:** Provide a structural index of the repository.

Include:

- directories and their purpose
- Django application boundaries (within `webclient/`)
- modules and their responsibilities
- file sizes and types
- infrastructure components (ELK stack, Prometheus, Redis, Fedora)

Note: The repository contains not only Django code but also a full infrastructure
configuration (elasticsearch/, kibana/, logstash/, prometheus/, redis/, fedora/, proxy/).
Record the responsibility of each directory.

Update whenever the repository structure changes.

---

## DEPENDENCY GRAPH (T02)

Create: `.agents/analysis/dependency_graph.json`

**Purpose:** Map internal and external dependencies.

Analyze:

- Python imports between Django applications in `webclient/`
- Django application dependencies (INSTALLED_APPS, cross-app imports)
  - Use `webclient/webclient/settings/base.py` → `INSTALLED_APPS` as the
    authoritative list of Django applications.
  - Analyse only production Python files — exclude `tests/` directories from
    the import graph. Tests may import any app; such imports must not be
    counted as architectural dependencies.
  - Distinguish **module-level imports** (load-time, high risk for circular
    dependency) from **lazy imports** (inside functions — lower risk, but
    still an architectural smell worth noting separately).
- Docker service dependencies (does X depend on Y at startup?)
- external libraries from `requirements.txt` and `pyproject.toml`
- npm dependencies from `package.json` (if absent, record as N/A and delegate
  CDN dependency analysis to T07)

Detect:

- circular dependencies between Django applications
  - **module-level** circular imports → severity Vysoká
  - **lazy** circular imports (import inside function) → severity Střední
- tightly coupled modules
  - flag any module with **fan-in > 10** as a decomposition candidate
  - flag any module with **fan-out > 6** as a high-responsibility candidate
- modules with excessive responsibilities
- outdated dependencies (versions)

Record architectural issues in `refactoring_backlog.md`.

```json
{
  "django_apps": {},
  "python_dependencies": {},
  "npm_dependencies": {},
  "docker_service_dependencies": {},
  "circular_dependencies": [],
  "architectural_issues": []
}
```

---

## DJANGO ORM ANALYSIS (T03)

Create: `.agents/analysis/orm_analysis.json`

**Purpose:** Analyse database usage and ORM patterns.

Inspect:

- Django models in each application — **read every `models.py` directly** (do not rely on summaries)
- migrations (count, state, squash candidates)
- queryset patterns in views, serializers and management commands
- usage of `select_related` / `prefetch_related`
- `signals.py` files — signals trigger `save()` chains that can cause hidden N+1 queries
- `managers.py` files — custom QuerySet managers affect ORM behaviour throughout the app
- `historie/models.py` — HistorieVazby and Historie are used by virtually all models

Detect:

- N+1 queries
- `len(queryset.all())` instead of `.count()` — common anti-pattern, always flag
- missing `select_related` / `prefetch_related`
- missing initial-value caching in `__init__()`: if a model's `save()` does
  `Model.objects.get(pk=self.pk)` to detect field changes, it should instead save
  `self._initial_<field> = self.<field>` in `__init__()` and compare in `save()`
- imports from non-standard libraries where stdlib or Django equivalents exist
  (e.g. `cached_property` from `distlib.util` instead of `functools`)
- deprecated ORM methods: `.extra()` (deprecated Django 4.0), `raw()` without
  parameters, `select_related` called without field names on large querysets
- unindexed fields used in filters
- heavy ORM loops
- large tables without indexes
- queries in templates or property methods

Note: For the mapping of AMČR domain entities (Projekt, Akce, Lokalita, Dokument,
PAS, Knihovna3D, PIAN, Hesláře, Uživatel, Organizace) to Django app directories, see
`review_config.yaml` → `key_django_apps`.

Warning: The `core` application is the largest in the project (77+ Python files,
26+ migrations, fan-in 16+). Always treat it as a separate sub-task (e.g. T03a)
and do not combine it with other apps in the same sub-task.

Note: The repository uses a special `urgent` database (Django DB router) for sequence
generation models (ProjektSekvence, AkceSekvence, PianSekvence). This is intentional
for concurrent safety — do not flag it as a misconfiguration.

Record severe ORM issues as bugs in `bugs.md`.
Cross-reference every bug entry with existing GitHub Issues (the repository has 113 open
issues; aktuální počet a datum viz `review_config.yaml` → `open_issues`) **before** writing the entry — see the BUG TRACKING section for the procedure.

```json
{
  "models": {
    "app.ModelName": {
      "fields_count": 0,
      "indexed_fields": [],
      "potential_missing_indexes": [],
      "foreign_keys": [],
      "migration_count": 0
    }
  },
  "query_patterns": [],
  "n_plus_one_candidates": [],
  "missing_prefetch_candidates": []
}
```

---

## DOCKER BUILD ANALYSIS (T04)

Create: `.agents/analysis/docker_analysis.json`

**Purpose:** Analyse Docker configuration and build efficiency.

Inspect ALL docker files:

- `Dockerfile` — production image
- `Dockerfile-DB` — development database
- `Dockerfile-DEV` — development environment
- `docker-compose.yml` — production stack
- `docker-compose-dev-local-db.yml` — local development
- `docker-compose-dev-local-db-all-containers.yml`
- `docker-compose-proxy.yml` — NGINX proxy
- `docker-compose-test.yml` — Selenium tests
- `git_docker-compose*.yml` — deployment from local repository
- directories: `proxy/`, `redis/`, `elasticsearch/`, `kibana/`, `logstash/`, `prometheus/`

Detect:

- unnecessarily large images (missing multi-stage build)
- build dependencies present in the production image
- poor layer cache utilisation
- outdated base images
- security risks in base images
- unnecessary installed packages
- inconsistencies between dev and production configurations
- hardcoded secrets or passwords
- incorrect secret injection patterns: for every service reading Docker secrets, verify
  the correct mechanism is used — Grafana (`GF_*__FILE` suffix), PostgreSQL
  (`POSTGRES_PASSWORD_FILE`), Redis (entrypoint sed), Elasticsearch/Logstash
  (entrypoint wrapper script — these do NOT support `_FILE` variants)
- version parity between dev and prod: compare base image versions of shared services
  (ELK, Prometheus, Grafana, Selenium) across all compose files; major version gaps
  → severity Střední
- services misplaced in wrong environment: test tools (Selenium) must not appear in
  the production compose; dev-only services must not appear in prod
- multi-process containers without PID 1 management: if a container runs two or more
  processes (`cmd1 & cmd2`), verify it uses a process supervisor (tini, s6-overlay)
  or the exec pattern in an entrypoint script
- monitoring and admin interfaces (Grafana, Prometheus, Kibana, Elasticsearch)
  exposed publicly without network isolation or authentication

```json
{
  "dockerfiles": [],
  "docker_compose_services": {},
  "base_images": {},
  "security_issues": [],
  "optimization_opportunities": []
}
```

---

## SECURITY ANALYSIS (T05)

Create: `.agents/analysis/security_analysis.json`

**Purpose:** Security audit of the production system.

For security-sensitive changes (auth, secrets, CORS, CSRF, XSS), consider
using a **security-reviewer** subagent before merge; document findings in
`.agents/analysis/security_analysis.json` and `bugs.md` / `refactoring_backlog.md`
as usual.

> **Cross-reference T04:** Docker-level security findings (secret injection errors,
> container privilege escalation, exposed monitoring ports) are already recorded in
> `.agents/analysis/docker_analysis.json` from T04. Do not duplicate those entries here —
> reference them by ID (SEC-D01 … SEC-D06) if they overlap with Django-level concerns.

Inspect:

- Run `python manage.py check --deploy` first and record all warnings
  (covers `DEBUG`, `SECRET_KEY`, `ALLOWED_HOSTS`, HTTPS settings, cookie security)
- Django security settings (`SECRET_KEY`, `DEBUG`, `ALLOWED_HOSTS`, `SECURE_*`);
  pay attention to fallback values in `get_secret()` calls — a fallback of `"True"`
  for `DEBUG` is as dangerous as hardcoding `DEBUG = True`
- secrets management (Docker secrets vs env vars vs files); when auditing
  committed sample secrets files, distinguish placeholder values
  ("changeme", "test_key", "PLACEHOLDER", "your_key_here", "secret") from
  potentially real credentials (random-looking hex/base64 strings, third-party
  domain URLs, email addresses) — real-looking credentials → Střední, recommend rotation
- authentication and authorisation (CAS integration, Django permissions);
  verify that any custom `user_can_authenticate()` backend returns `False`
  (not raises an exception) for inactive users
- NGINX proxy configuration (`proxy/default.conf`) — HTTPS redirect, HSTS,
  `X-Frame-Options`, `Content-Security-Policy` may be set at proxy level; check
  whether Django-level `SECURE_*` settings are still needed (defense-in-depth)
- `webclient/webclient/urls.py` — check admin URL path predictability, exposed
  debug views
- `webclient/core/middleware.py` (or `middleware/` directory if split) — custom
  middleware can introduce or remove security controls
- CORS configuration
- CSRF protection
- SQL injection risks (raw queries, `extra()`, `RawSQL()`)
- XSS risks (`safe` filters in templates, `mark_safe()`):
  - **Acceptable:** hardcoded HTML string without user input (e.g. widget decoration)
  - **Střední risk:** value from a model property or DB attribute without explicit `escape()`
  - **Vysoká risk:** value directly from request parameters or user input
  - Preferred pattern: `format_html()` instead of `mark_safe()` for dynamic content
- dependencies with known CVEs — primary method: run `pip audit` or
  `safety check -r requirements.txt`; if tools unavailable in offline environment,
  record as "CVE audit recommended" and add to `refactoring_backlog.md` as a SEC step
- sensitive files in `.gitignore` vs what is actually committed

> **Note on `cert/`:** This directory contains self-signed certificates for local
> development only. They are intentionally committed and are not a production
> security concern. Do not treat them as a finding — do not inspect them.

Severity mapping:

- `DEBUG=True` in production → Kritická
- `get_secret("DEBUG", "True")` — dangerous fallback → Vysoká
- hardcoded `SECRET_KEY` → Kritická
- outdated dependency with CVE → Vysoká
- missing CSRF protection → Vysoká
- `mark_safe()` with DB/request value without `escape()` → Střední
- unnecessary `safe` filter → Střední
- real-looking credentials in committed sample file → Střední

```json
{
  "django_security_settings": {},
  "secrets_management": {},
  "authentication": {},
  "known_vulnerabilities": [],
  "csrf_xss_issues": [],
  "raw_sql_usage": []
}
```

---

## CELERY ANALYSIS (T06)

Create: `.agents/analysis/celery_analysis.json`

**Purpose:** Analyse asynchronous task processing.

Inspect:

- definitions of all Celery tasks (`tasks.py` files)
- beat schedule configuration
  - when `django_celery_beat` is used, only record that the schedule is stored in the database (not fully visible in the repository) and briefly summarise the types of periodic tasks; do not try to reconstruct the full schedule from code alone
- retry strategies and error handling
- shared state between tasks (dangerous global variables)
- timeout configuration
- task routing and priority queues

Detect:

- tasks without error handling
- potential race conditions
- excessively long synchronous operations inside tasks
- missing idempotence in critical tasks
- tasks called synchronously where they should be async
- memory leaks in long-running tasks
- calls to external services (HTTP, file systems, external APIs) without an explicit timeout or without a retry strategy for critical tasks
- utility or simple wrapper tasks (e.g. write_value_to_redis) may be summarized per module rather than fully described; focus detailed description on domain-significant tasks

```json
{
  "tasks": [],
  "beat_schedule": {},
  "configuration": {},
  "error_handling_issues": [],
  "race_condition_candidates": [],
  "missing_idempotence": [],
  "timeout_issues": []
}
```

---

## FRONTEND ANALYSIS (T07)

Create: `.agents/analysis/frontend_analysis.json`

**Purpose:** Analyse **custom** frontend code written by AMČR developers
(JavaScript accounts for ~37 % of the repository).

### Scope

**Analyse only code written by AMČR developers.** Statically embedded third-party
libraries that developers have not modified are out of scope and must not be evaluated.

**Vendored file detection:** Skip files matching `review_config.yaml` →
`vendored_exclusions` (directories, file patterns, copyright headers, explicit
filenames). Also skip unmodified production dependencies from `package.json`.
Run `review_tools.py coverage-gaps` to verify classification.

**Analyse these files:**

- `package.json` — build scripts, dependency management
- custom JavaScript in `static/` outside vendored subdirectories
- custom SCSS/CSS in `static/` (files without third-party copyright headers)
- Django templates in `templates/` — inline `<script>` blocks and inline styles
  written by AMČR developers
- bundling configuration (webpack/parcel) if present

### What to inspect

- custom JS architecture: modularity, separation of concerns
- async/await vs callback patterns in custom code
- inline scripts in templates — quantity and complexity; larger inline blocks in core templates (for example `base.html`) should be treated as candidates for extraction into separate JS files and listed in `template_inline_scripts.extraction_candidates`
- Django integration patterns (CSRF tokens, URL reversal in JS, data attributes)
- SCSS structure: variables, nesting, repeated patterns
- how CDN dependencies are loaded (integrity hash / SRI, fallback)
  - for first‑party analytics tools (for example Google Tag Manager, Google Analytics), document missing SRI but do not treat it as a defect; the risk decision belongs to the project security policy

### What to detect

- logic errors or anti-patterns in custom JS code
- inline scripts in templates suitable for extraction into separate files
- missing SRI integrity attributes on `<script src="...">` and `<link>` tags
  loaded from external CDNs (this concerns template configuration, not library content)
- missing minification of custom JS/CSS in the production build
- direct DOM manipulations that could be replaced by a Django mechanism
- inconsistent patterns for passing data from server to JS
- how dark mode is implemented (if present) — how it is toggled (`prefers-color-scheme` vs. UI button), how it maps to CSS (`data-theme`, dedicated SCSS layer) and how it is reflected in `frontend_analysis.json`

### What NOT to address

- code quality, API or bugs inside vendored libraries
- versioning or content of files such as `jquery.min.js`, `bootstrap.min.js`
  and similar statically embedded bundles
- npm dependencies as such (their security audit belongs to T05)

```json
{
  "scope_note": "AMČR developer code only; vendored libraries excluded.",
  "vendored_paths_skipped": [],
  "custom_javascript": {
    "files_analyzed": [],
    "patterns": [],
    "issues": []
  },
  "custom_scss": {
    "files_analyzed": [],
    "structure_notes": "",
    "issues": []
  },
  "template_inline_scripts": {
    "count": 0,
    "extraction_candidates": []
  },
  "cdn_sri_issues": [],
  "django_js_integration": {
    "csrf_handling": "",
    "data_passing_patterns": [],
    "issues": []
  },
  "build_pipeline": {
    "tool": "",
    "custom_assets_minified": null,
    "issues": []
  }
}
```

---

## DOCUMENTATION ANALYSIS (T08)

Create: `.agents/analysis/documentation_analysis.json`

**Purpose:** Analyse documentation quality and documentation generators.

Inspect:

- Sphinx documentation in `docs/`
- `readthedocs.yaml` — Read the Docs build configuration
- reStructuredText files
- documentation generators (for example `docs/generate_module_docs.py`, `docs/generate_selenium_test_docs.py`, `docs/licenses/convert_to_rst.py`), including those that use `subprocess` (check `returncode` and how stderr/stdout are handled)
- automatically generated dependency tables
- licence documentation

Detect:

- documentation generators that may fail
- fragile parsing logic without error handling
- outdated generated tables
- duplicated documentation
- broken or outdated links
- inconsistent formatting
- documentation that does not match current code behaviour
  - for Selenium documentation, check whether the generator enforces required sections (for example `Steps`, `Expected`) and how it behaves when they are missing; record missing sections under `coverage_gaps` with a recommendation for CI (for example failing the build on incomplete documentation)

```json
{
  "sphinx_docs": {
    "location": "docs/",
    "readthedocs_config": "docs/source/conf.py",
    "readthedocs_url": "https://aiscr-webamcr.readthedocs.io/cs/stable/",
    "issues": []
  },
  "documentation_generators": [],
  "broken_links": [],
  "coverage_gaps": [],
  "outdated_content": []
}
```

---

## CI/CD ANALYSIS (T09)

Create: `.agents/analysis/cicd_analysis.json`

**Purpose:** Analyse automation, testing and deployment pipeline.

Inspect:

- `.github/workflows/` — all GitHub Actions workflows
- `.pre-commit-config.yaml` — pre-commit hooks
- `.flake8` — linting configuration
- `.isort.cfg` — import sorting configuration
- test infrastructure (`docker-compose-test.yml`, Selenium tests)
- security scanning and supply-chain tooling:
  - image-level scanning (Docker Scout, Trivy, SBOM generators, cosign signing, provenance)
  - static code analysis (CodeQL, Bandit, etc.), if present
  - Dependabot configuration (`.github/dependabot.yml`) for Python dependencies and GitHub Actions

Detect:

- workflows without timeouts (risk: infinitely running jobs)
- missing test coverage reporting
- secrets exposed in CI logs
- missing branch protection rules
- suboptimal use of GitHub Actions cache
- pre-commit hooks that may fail in CI
- missing security scanning (Dependabot, CodeQL, image-level scanning)

```json
{
  "workflows": [],
  "pre_commit_hooks": [],
  "linting_config": {},
  "test_infrastructure": {},
  "security_scanning": {},
  "issues": []
}
```

---

## SCRIPTS ANALYSIS (T10)

Create: `.agents/analysis/scripts_analysis.json`

**Purpose:** Analyse operational and development scripts.

**Scope:** T10 covers only the `scripts/` directory in the repository root. Scripts under `docs/` (e.g. documentation generators such as `generate_module_docs.py`, `generate_selenium_test_docs.py`) are in scope of T08 (Documentation Analysis), not T10.

Inspect everything in `scripts/`:

- deploy scripts (`dev_deploy.sh` and others)
- maintenance scripts
- Python scripts for documentation generation
- database scripts

**Config files** (e.g. `crontab.txt`, `uwsgi_site.ini`): Do not analyse them line-by-line as executable scripts. Document their relationship to scripts (what they run, how often) and any risks (e.g. missing logging or error handling in invoked commands).

Detect:

- scripts without error handling (missing `set -e`); where appropriate check for `set -u` and `set -o pipefail`
- for scripts that perform destructive operations (e.g. DROP DATABASE, `rm -rf`, file overwrites), verify that required environment variables or arguments are validated before use
- hardcoded paths or hostnames
- scripts that assume a specific environment without validation
- duplicated functionality across scripts
- scripts without documentation (help text)

Record findings in `scripts_analysis.json`. Each entry in the `issues` array must have: `id` (string, e.g. T10-SH-01), `severity` (e.g. Střední), `summary` (short description), `scripts` (array of script paths), `recommendation` (text).

---

## REVIEW CACHE

Create and maintain: `.agents/config/review_cache.json`

```json
{
  "schema_version": "2",
  "last_updated": "<iso8601>",
  "tasks": {
    "T01": {
      "status": "pending|done|skipped",
      "completed_at": null,
      "report_path": null
    },
    "T03": {
      "status": "pending|done|split",
      "completed_at": null,
      "report_path": null,
      "sub_tasks": {
        "T03a": {
          "status": "pending|done",
          "completed_at": null,
          "scope": "webclient/<app_name>",
          "report_path": null
        }
      }
    }
  },
  "file_hashes": {
    "<relative_path>": {
      "sha256": "<hash>",
      "last_reviewed": "<iso8601>",
      "task_id": "<T0X>"
    }
  }
}
```

---

## BUG TRACKING

Create and maintain: `.agents/reports/bugs.md`

Before adding a bug entry:

1. Check if a related GitHub Issue exists (for current number of open issues see
   `review_config.yaml` → `open_issues`; can be updated during the session run).
2. If yes → mark as "již evidováno (Issue #XXX)".
3. If partially related → mark as "rozšíření existujícího issue #XXX".
4. If none exists → mark as "nový kandidát na issue".

Severity levels: `Kritická | Vysoká | Střední | Nízká`

When a bug also has a corresponding entry in `refactoring_backlog.md`, ensure the
severity in `bugs.md` and the priority section in the backlog are reconciled. If
they intentionally differ (e.g. refactoring priority reflects business impact while
bug severity reflects technical risk), document the rationale in the bug entry.

Each bug entry:

```markdown
### BUG-XXX: Stručný popis

- **Soubor:** `path/to/file.py:line`
- **Závažnost:** Kritická
- **GitHub Issue:** #123 — již evidováno / nový kandidát
- **Popis:** Co je špatně a proč to je problém.
- **Navrhovaná oprava:** Konkrétní kroky k opravě.
- **Task:** T0X
```

### Escalation for Critical severity

When a bug of severity `Kritická` is found:

1. Record it in `bugs.md` as usual.
2. Prepend the following block to the current session summary (do not bury it
   in the report body):

   ```markdown
   ⚠️  KRITICKÁ CHYBA — BUG-XXX: <stručný popis>
   Viz .agents/reports/bugs.md. Před dalším taskem doporučeno lidské review.
   ```

3. Do not start the next task in the same session until the finding is recorded
   and the summary block is written.

---

## REFACTORING BACKLOG

Create and maintain: `.agents/reports/refactoring_backlog.md`

```markdown
# Refactoring backlog

## Vysoká priorita
<!-- Architektonické problémy, bezpečnostní dluhy, ORM výkon -->

## Střední priorita
<!-- Optimalizace, dekompozice modulů, Docker build -->

## Nízká priorita
<!-- Kosmetické úpravy, dokumentace, minor code quality -->
```

When adding an item with a `[TXX]` prefix, verify the finding was actually
discovered during task TXX. The prefix must match the source task. Cross-validate
before committing — incorrect prefixes create orphan references during
cross-validation.

---

## REPORT OUTPUT

Each completed task must produce: `.agents/reports/review_reports/<task_id>.md`

The report must be written in Czech and include:

**Datum** and any timestamps in analysis JSON or cache must be set per IMPORTANT RULES rule 4 (git or Python, never guessed).

```markdown
# <Task ID> — <Název úlohy>

**Datum:** <iso8601, see rule 4>
**Dotčené soubory:** <seznam>

## 1. Shrnutí zjištění

## 2. Identifikované problémy

## 3. Návrhy zlepšení

## 4. Odhad náročnosti refaktoringu

| Oblast | Náročnost | Priorita |
|--------|-----------|----------|
| ...    | S/M/L/XL  | 1–5      |

## 5. Návrhy na zlepšení promptu
<!-- Sekce pro prompt evolution -->
```

Also update relevant analysis JSON files, `bugs.md`, and `refactoring_backlog.md`.

---

## PROMPT EVOLUTION

At the end of each task report, include a section:

```markdown
## Návrhy na zlepšení promptu

- Co v promptu chybělo / bylo nejasné
- Co by příštímu agentovi pomohlo
- Jaké soubory nebo adresáře by stálo za to přidat
```

Save to: `.agents/prompts/prompt_evolution/<task_id>_prompt_update.md`

Suggestions accumulate across sessions. A human reviewer applies them to
`review_codebase.md`. Agents must not self-modify `review_codebase.md` without explicit approval by human.

For incremental follow-up reviews, use `.agents/prompts/review_update.md`.

---

## FINAL AUDIT (T11)

When all tasks T01–T10 are completed, create: `.agents/reports/review_reports/final_audit.md`

The final report must include:

```markdown
# Finální audit — AMČR (aiscr-webamcr)

## 1. Hlavní architektonické problémy
## 2. Bezpečnostní rizika (prioritizovaná)
## 3. Cirkulární a problematické závislosti
## 4. Moduly s vysokou komplexitou
## 5. ORM výkonové problémy
## 6. Databázová rizika
## 7. Docker build problémy
## 8. Frontend a JavaScript rizika
## 9. Celery a asynchronní rizika
## 10. Problémy v dokumentačních generátorech
## 11. CI/CD mezery
## 12. Hlavní technický dluh (TOP 10)
## 13. Prioritizovaný plán refaktoringu
## 14. Doporučení pro dlouhodobou správu repozitáře
## Changelog
```

Before finalizing, run `review_tools.py all` to detect inconsistencies
(orphan IDs, severity mismatches, missing report paths). Then manually verify:

1. All SEC-\* IDs are at least referenced; lower-severity IDs may be summarized
   with a pointer to the source JSON.
2. Summary tables use canonical backlog IDs (SCRIPTS-XX, ORM-XX) alongside
   analysis JSON IDs (T10-SH-XX, NP1-XXX).

The final audit must include a `## Changelog` section. Incremental updates
(via `review_update.md`) append entries there instead of creating separate files.

---

## TASK EXECUTION RULES

1. Always read `AGENTS.md` first — it may override instructions here.
2. Always load cache before starting — never repeat completed tasks.
3. Analyse only files relevant to the current task.
4. Respect task size limits (`max_lines_per_task`, `max_lines_per_file`) — split large tasks if needed.
5. Always update cache and analysis JSON files after completing a task.
6. Cross-reference all bugs with existing GitHub Issues before filing.
7. When a Django app is large, create one sub-task per app (e.g., T03a, T03b).
8. Infrastructure components (ELK, Prometheus, Redis, Fedora) are in scope for T04.
9. **DONE MEANS DONE** — never mark `done` until every file in scope was
   directly Read (not via sub-agent summary, Grep, or dir listing). If budget
   exhausted: split into sub-tasks, mark completed part `done`, parent `split`,
   remainder `pending`.
10. After T11, run `review_tools.py all` and fix any errors before marking done.
    Use `review_tools.py --help` for all available commands.
