[bootstrap]
schema_in_use = "spec-driven"
last_aligned_at = "2026-08-22"
last_aligned_hub_revision = "b4385754bc9e4665e61f9dd6cb1f099852598caa"
mode = "bootstrap-mode-align"
target_branch = "DN-agents-setup-review"

[bootstrap.notes]
summary = "Align run: reconciled the sibling-owned entry docs against freshly rendered hub candidates (AGENTS governance kernel, CLAUDE.md vendor-digest blocks added, CODEX.md generated rule sections refreshed off a superseded generation, GEMINI.md and .github/copilot-instructions.md preserve-only digest blocks refreshed); corrected retired workflow slugs and the review_config file extension in AGENTS.md, CONTRIBUTING.md, and the sibling-owned Copilot enrolled-workflow list; replaced the retired specialized-sync-assets registry reference in openspec/config.yaml context; aligned README audience tiering with the hub (single English README.md outside the bilingual set, EN twins removed from the four vendor roots, their skills/ subdirectories, and .agents/reports plus review_reports); removed the superseded safety_config_templates tree. Delivery-baseline drift and retired-slug orphan removal were handed off to aiscr-setup-sibling-bundle rather than applied here."

[bootstrap.notes.open_items]
bundle_apply_pending = "Direct-bundle apply not run in this session: 158 expected paths (78 CONTENT, 79 NEW-M, 1 NEW-R) remain behind the hub baseline. Run /aiscr-setup-sibling-bundle."
orphan_cleanup_pending = "81 retired-hub-slug orphan paths remain under the vendor roots. sibling_governance_delivery.py cleanup, separate approval gate."
orphaned_companion_script = ".agents/scripts/port_workspace_safety_config.py consumed the removed safety_config_templates tree. Retired at the hub in favour of setup_workstation_assistant_safety.py, which the pending bundle delivers. Removal awaits a maintainer decision."
openspec_posture_undeclared = "repos.toml declares openspec_posture = change-tracked; this repository openspec/config.yaml context block declares no posture. Reported, not reconciled."
openspec_cli_version = "@fission-ai/openspec pinned at 1.6.0 here, 1.10.0 at the hub. Advisory."
quality_baseline_gaps = ".editorconfig absent; .gitattributes treats 11 binary files (fonts, .pdf, .docx, .dwg) as text. Owned by aiscr-setup-quality-baseline, not by bootstrap."
