# aiscr:canonical=.agents/canonical_configs/workflow_skills/aiscr-workstation-assistant-sandbox.md
description = "Merge validated Codex and Claude safety templates into user app-level config; dry-run first, explicit confirmation; may pip-install jsonschema/tomli-w; optional --backup and --target-dir; not orchestrate_local_agent_sync apply nor retired sync_agent_configs operator path."
prompt = """
<!-- aiscr:canonical=.agents/canonical_configs/workflow_skills/aiscr-workstation-assistant-sandbox.md -->

<!-- aiscr:stop-anchor -->
**Entry scope**

- Stay in this `.gemini/commands/aiscr/` surface and its same-vendor pointers first.
- Do not open parallel vendor trees by default just in case.
- Cross into another vendor tree only for explicit parity checks, generator work, or governance maintenance.

Merge validated Codex and Claude safety templates into user app-level config; dry-run first, explicit confirmation; may pip-install jsonschema/tomli-w; optional --backup and --target-dir; not orchestrate_local_agent_sync apply nor retired sync_agent_configs operator path.

**Skill body (authoritative):** [`.gemini/skills/aiscr-workstation-assistant-sandbox/SKILL.md`](.gemini/skills/aiscr-workstation-assistant-sandbox/SKILL.md) — load that repository-local file for the full workflow body, guardrails, and verification.
"""
